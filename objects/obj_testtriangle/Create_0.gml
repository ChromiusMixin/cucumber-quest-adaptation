// Distance between center and vertex
distance = 100;

// Measured directly in radians for performance (1° = pi/180)
rotspeed = (pi / 360); 
base_angle = 0;

// Angle offsets
offset1 = (120 * pi / 180);
offset2 = (240 * pi / 180);

// Positions for vertices [A, B, C]
pos_x = [0, 0, 0];
pos_y = [0, 0, 0];